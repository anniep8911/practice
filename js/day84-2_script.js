$(function(){
    var $btnPrev=$('.overgrid .prev'),
        $btnNext=$('.overgrid .next'),
        $imgGroup = $('.overgrid .imgGroup'),
        $thumGroup =$('.overgrid .thmGroup');

        $btnPrev.on('click',prev);
        $btnNext.on('click',next);

        function prev(){
        }
        function next(){
           
        }
});